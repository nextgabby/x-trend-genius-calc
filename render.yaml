services:
  - type: web
    name: trend-genius-calc
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: X_API_BEARER_TOKEN
        sync: false
      - key: GROK_API_KEY
        sync: false
